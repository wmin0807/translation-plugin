# Changelog

## 0.2.0 - 2026-09-05

- 新增扩展图标：紫蓝渐变 "FF" 标志加"译"字角标，注册到工具栏和扩展管理页。

- 新增自定义输入翻译：未选中任何文字时连续按两次 `F`，在页面中央弹出输入框，输入内容后按 `Cmd/Ctrl + Enter` 或点击"翻译"即可翻译。
- 翻译结果流式显示在同一弹窗内，原文保留，可修改后重新翻译。
- 在网页输入框内触发时自动清理多敲入的 `f` 字符。
- 关闭自定义翻译框后，自动把焦点和光标位置恢复回之前的输入框，可继续输入。

## 0.1.1 - 2026-08-30

- 兼容中文输入法状态下的物理 `F` 键识别。
- 安装或更新后尝试向已经打开的普通网页补充注入内容脚本。
- 增加重复注入保护。

## 0.1.0 - 2026-08-30

- 首个可用版本。
- 支持 `FF` 划词翻译和流式结果浮层。
- 默认支持 DeepSeek V4 Flash。
- 支持自定义 OpenAI Chat Completions 兼容服务。
- API Key 和设置仅保存在浏览器本地。
